<!--
Add this line to the target repo's CLAUDE.md (create the file if absent) so
Claude Code reads the shared repo contract. The `@` include pulls AGENTS.md in
verbatim, keeping a single source of truth that both vendors share.
-->
@AGENTS.md
